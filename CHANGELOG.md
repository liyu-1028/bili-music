# 更新日志

本文件记录 Bili Music 的版本变更。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]


## [0.6.0] - 2026-09-09

### 新增

- **动态背景主题**：从当前播放歌曲的封面提取主色，生成缓慢流动的渐变背景，换歌时平滑过渡。
- **主题色可选**：提供粉、蓝、紫、绿、橙、青六种主题色，每种在深色与浅色主题下各有适配变体，设置中可随时切换。
- **导入 B站收藏夹**：在「我的歌单」输入公开收藏夹链接，可将其中的视频批量导入为本地歌单，自动跳过失效条目，单次上限 200 首。
- **侧栏宽度可调**：拖拽侧栏右边界调整宽度，拖到阈值以下自动折叠为图标模式，也可用底部按钮一键收起，宽度与折叠状态会被记住。

### 变更

- **沉浸页重排**：封面放大并适配 B站 16:10 比例，标题与控件相应收敛，歌词区上下新增渐隐效果。
- **列表视觉层级**：歌曲标题加粗放大作为视觉锚点，播放量与时长退为辅助信息并右对齐，边界不再参差。
- 首页顶部间距收紧，推荐列表可见条目更多。
- 全站强调色统一由 CSS 变量驱动，为主题色切换打好基础。

### 修复

- **浅色主题多项问题**：修复正在播放行的高亮被行底色覆盖、列表行 hover 无背景反馈、以及整体对比度过强导致长时间阅读疲劳的问题。
- 修复列表选中行背景在操作按钮区域出现断层的问题。
- **macOS 播放兼容性**：修复部分歌曲在 macOS 上无法播放的三类问题——新投稿无 DASH 音轨、mcdn 边缘节点漂移、以及 octet-stream 媒体类型被 WKWebView 拒绝解码。（@liyu-1028）

### 已知问题

- 动态背景与背景图主题存在一定的 GPU 占用，性能敏感的设备建议使用深色或浅色主题。
- 动态背景与主题色在 macOS 上尚未充分验证。


## [0.5.0] - 2026-09-05

### 新增

- **Windows 任务栏控制按钮**：鼠标悬停任务栏图标，在缩略图预览下方可直接上一首 / 播放暂停 / 下一首（#3）。
- **搜索排序模式**：新增「单曲 / 合集」筛选，可按需要优先看单曲或长合集。
- **拖拽排序**：歌单列表、歌单内歌曲、我的收藏均支持拖动调整顺序（#5）。
- **播放条加入歌单**：可直接把当前播放的歌曲加入歌单，无需回到列表。

### 变更

- **搜索相关性重排**：结果按标题匹配度、时长合理性、精确短语命中本地重新排序，原曲上浮，长合集与泛匹配降权（#2）。
- **循环模式图标**：列表循环与单曲循环使用不同图标，不再只靠高亮区分（#4）。
- 搜索分区与排序合并为一行，移除 VOCALOID / 演奏 / 电台三个低频分区。
- 播放条布局重做：分P 与队列位置加 P / ♪ 标识、音量改为悬停展开、移除全屏按钮（点击封面即可进入沉浸页）。
- 沉浸页封面适配 B站 16:10 比例，不再左右裁切。

### 修复

- 修复分P 按钮在窄窗口下被媒体查询意外隐藏的问题。
- 清理编译警告；搜索诊断日志限定 debug 构建，不再进入发布版本。

## [0.4.0] - 2026-08-27

### 新增

- **恢复上次播放状态**：关闭应用后重新打开，播放队列与上次听到的位置会自动恢复，按播放键即可接着听（不会自动播放）。
- **macOS 标题栏适配**：macOS 上使用系统原生红绿灯按钮与沉浸式标题栏，Windows 行为不变。（@liyu-1028）

### 变更

- 项目许可协议由 PolyForm Strict 1.0.0 变更为 MIT License。
- 跨平台支持（macOS）：数据目录按平台解析，新增 Windows + macOS 双平台自动构建。（@liyu-1028）
- 发布流水线改为仅在推送 tag 时触发，避免日常提交重新生成产物。

## [0.3.4] - 2026-08-04

### 新增

- **分P 面板**：多P 视频（如歌曲合集）可自由选择从哪一首开始播放。列表项右侧显示分P 数角标，点击角标或双击条目即可打开面板；底部播放条显示「当前P/总数」入口，可随时跳转到其它分P。
- **推荐结果保持**：生成的推荐列表会保存到 `%APPDATA%\bili-music\recommendations.json`，重启应用后依然是上次的结果，只有点击「生成推荐」或「刷新」才会重新生成。
- **未配置 Key 时展示热门音乐**：没有填写 API Key 时，首页展示 B站 音乐热门榜，不再空白。
- **分P 信息本地缓存**：分P 数量与完整分P 列表缓存到 `video-pages-cache.json`（30 天 TTL），重启后角标直接显示，分P 面板零请求打开。

### 修复

- 单击正在播放的条目不再从头重新播放（多P 视频不会跳回第 1P）。
- AI 请求区分长短任务超时（测试连接 15s、生成任务 90s，新增 10s 连接超时），缓解生成推荐偶发失败。
- 生成推荐失败或返回空结果时，不再清空已保存的推荐。
- 前端推荐请求超时由 45s 调整为 95s，与后端对齐。

## [0.3.3] - 2026-08-02

### 新增

- **按歌名手动匹配歌词**：自动匹配不准时，可直接输入歌名（可加歌手）搜索并从结果中选择，不再需要查找歌曲 id；输入框会自动预填当前视频标题。
- **歌词本地缓存**：匹配过的歌词按歌曲 id 缓存到 `%APPDATA%\bili-music\lyrics\`，再次播放无需重新请求；网络异常或接口不可用时，已缓存的歌词仍可正常显示。

### 改进

- **匹配区自动隐藏**：歌词匹配成功后，上方匹配提示自动收起，不干扰阅读；鼠标移到歌词顶部即可唤回。候选确认与手动搜索状态下不会自动隐藏。

### 修复

- 修复手动搜索结果的嵌套滚动条与输入框焦点边框显示不完整的问题。
- 修复匹配区隐藏后仍占据空间、导致歌词上方留白的问题。

## [0.3.2] - 2026-07-30

### 新增

- **沉浸页歌词**：播放时自动识别歌曲并显示滚动歌词，当前行高亮居中。
- **自动匹配**：依次利用 B站 BGM 标签、视频简介与分P 标题构造关键词，匹配到 QQ 音乐歌曲后获取歌词；高置信度自动采用，存在多个可能结果时列出候选供确认，纯音乐与合集类视频静默降级。
- **歌词校准**：提供 −0.5s / +0.5s 偏移按钮，校准结果按视频与分P 分别记住（`lyrics-offsets.json`），下次播放自动恢复。
- **同步 / 全文切换**：可在逐行同步与全文浏览之间切换。
- **手动匹配与重新匹配**：自动匹配不准时可手动指定歌曲或重新匹配，绑定结果保存在 `lyrics-bindings.json`。
- **加入歌单**：沉浸页新增「加入歌单」入口。

### 改进

- **沉浸页重排**：宽窗下改为左右两栏——左侧集中封面、歌曲信息与全部控件，右侧完整留给歌词。
- 歌词匹配与校准数据保存在 `%APPDATA%\bili-music\`，随「数据与隐私 → 导出」一并备份。

## [0.3.1] - 2026-07-24

### 修复

- 「测试连接」不再因单 token 回复被截空而误报「响应缺少 content」，有效的 API Key 能正确通过测试。
- 兼容推理型模型：当模型把回复放在 `reasoning_content`（如 DeepSeek reasoner / flash 系列）时，推荐功能可正常解析。

### 新增

- 外观新增「内容区背景」滑块：统一调节首页 / 搜索 / 收藏 / 歌单内容区在背景图主题下的背景浓度，默认透明。

## [0.3.0] - 2026-07-10

本次是一轮体验与数据架构的整体打磨：首页更聚焦、数据更干净、设置更清爽。

### 新增

- **数据一键导入 / 导出**：在「设置 → 数据与隐私」可把收藏、歌单、听歌记录、AI 配置与背景图打包成一个 zip，换机或重装一键导回。
- **桌宠可隐藏**：桌宠选择器新增「不显示」；桌宠陪伴话术也大幅扩充，更少重复。

### 改进

- **首页重构**：聚焦「为你推荐」——标题区精简、一句话输入独立成卡、推荐列表单独成块；空状态改为轻量居中提示。
- **生成推荐加载反馈**：点「生成推荐」后按钮即时显示「生成中…」，不再让人以为没反应。
- **设置面板整理**：背景相关项仅在「背景图」主题下显示；AI 推荐、数据与隐私折叠收纳；取流方案文案改为更好懂的「音频获取方式」。

### 修复

- **背景图丢失**：选背景后会把图复制进应用数据目录，删除原图不再导致背景消失。
- **运行目录零残留**：所有运行期数据/配置改存系统用户目录 `%APPDATA%\bili-music\`，程序目录不再散落 JSON 文件。

## [0.2.0] - 2026-06

### 新增

- AI 推荐（「为你推荐」）：基于收藏、搜索、歌单与听歌记录生成推荐，支持一句话描述当前想听的内容。
- 桌宠陪伴。

## [0.1.0] - 2026-05

首个公开版本。

### 新增

- B站 音频搜索与播放，支持 BV 号直达。
- 收藏、歌单、播放队列。
- 沉浸播放页、SMTC 系统媒体控件集成。
- 多主题外观（深色 / 浅色 / 背景图）与质感微调。

[未发布]: https://github.com/Jmiao11/bili-music/compare/v0.3.4...HEAD
[0.3.4]: https://github.com/Jmiao11/bili-music/compare/v0.3.3...v0.3.4
[0.3.3]: https://github.com/Jmiao11/bili-music/compare/v0.3.2...v0.3.3
[0.3.2]: https://github.com/Jmiao11/bili-music/compare/v0.3.1...v0.3.2
[0.3.1]: https://github.com/Jmiao11/bili-music/compare/v0.3.0...v0.3.1
[0.3.0]: https://github.com/Jmiao11/bili-music/compare/v0.2.0...v0.3.0
[0.2.0]: https://github.com/Jmiao11/bili-music/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/Jmiao11/bili-music/releases/tag/v0.1.0
